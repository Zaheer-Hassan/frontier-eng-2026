# Trace — advanced: Phase 3 plan-edit-verify agent beats baseline

| Field | Value |
|-------|--------|
| **ID** | `2026-08-29_1500_phase3-advanced` |
| **When (PKT)** | 2026-08-29 |
| **Duration** | ~50 min |
| **Agent(s)** | Grok (xAI) implementing; runtime agent = `ADVANCED/agent.py` |
| **Human** | Zaheer ul Hassan |
| **Phase** | advanced |
| **Related changelog** | `[2026-08-29] — Phase 3: advanced agent 100% (+62.5pp)` |
| **Status** | submission-ready |

---

## 0) System state before this session
- Baseline frozen at **37.5% (3/8)** via one-pass heuristics
- No API keys available for LLM product agent
- Need measurable lift on same 8 cases, including hard password case

## 1) Session goal (falsifiable)
**Done means:** advanced agent with plan/edit/verify/retry; success_rate **> 37.5%** on all 8 cases; metrics + per-case trajectories saved.

## 2) Constraints
- Same eval cases / metric as baseline
- Do not mutate pristine fixtures
- Prefer architecture that shows agentic structure even without cloud LLM keys

## 3) Plan (before coding)
1. Factor repair skills into named strategies
2. Implement planner that ranks unused strategies from pytest/source signals
3. Agent loop with retry budget
4. Eval all cases; compare to baseline JSON
5. Docs + changelog + push

## 4) Agent brief (prompt issued)
```
Start Phase 3
```

**Why this brief:** Deliver advanced loop and prove delta vs 37.5%.

## 5) Execution log
| Step | Actor | Action | Result |
|------|-------|--------|--------|
| 1 | agent | Design strategy bank + planner | ok |
| 2 | agent | Implement `ADVANCED/agent.py` loop | ok |
| 3 | agent | Run `python ADVANCED/agent.py` | **8/8 = 100%** |
| 4 | agent | Compare to baseline metrics | **+62.5 pp** |
| 5 | agent | Persist `trajectories/*.json` + metrics | ok |

**Commands / checks run:**
```bash
python3 ADVANCED/agent.py
# advanced success_rate=100.00% (8/8) delta_vs_baseline=+62.50%
```

## 6) Verification
- [x] Same 8 cases
- [x] Hard case passed
- [x] Per-case trajectories written
- [x] Delta vs baseline recorded in metrics JSON

**Evidence:** `ADVANCED/results/advanced_metrics.json`, `ADVANCED/trajectories/`

## 7) Decision record
| Decision | Chose | Rejected | Why |
|----------|-------|----------|-----|
| Advanced form | Tool loop + strategy skills + retries | Wait for API / LLM-only agent | Keys missing; still demonstrates agentic control loop |
| Skill factorization | Named strategies with planner | One mega-regex | Clear trajectories; easier ablation |
| Spoiler policy | Strip meta.json in work copy | Keep spoilers | Fairer tool use |

**Tradeoff accepted:** Strategies are hand-authored skills (like agent tools), not learned weights. Honesty: improvement comes from better control loop + broader skill coverage.  
**Revisit if:** API keys arrive — can wrap LLM proposal as an additional strategy without changing eval protocol.

## 8) Failure / incident (experiment removed)
- **Tried first idea:** “Just re-run baseline heuristics in a loop” — rejected because without new skills it cannot fix semantic cases; would fake “retries” without capability gain.
- Kept instead: expand skill catalog + planner selection + verify gate.

## 9) Risk still open
- Judges may ask for LLM-in-the-loop demo — optional oneshot path exists; keys needed
- Strategy bank is domain-narrow to this sample world (acceptable for sprint scope)

## 10) Handoff
**Next session starts with:** Phase 4 proof pack — polish REPRO, write-up/hot take, video script, trajectory audit.

---

### Quality gate
- [x] Goal clear and checked
- [x] Prompt preserved
- [x] Actions + evidence present
- [x] Decision/tradeoff + rejected experiment
- [x] Handoff actionable
- [x] No secrets
