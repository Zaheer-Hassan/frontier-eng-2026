# Trace — baseline: Phase 2 one-pass heuristic fixer + metrics

| Field | Value |
|-------|--------|
| **ID** | `2026-08-29_1430_phase2-baseline` |
| **When (PKT)** | 2026-08-29 |
| **Duration** | ~45 min |
| **Agent(s)** | Grok (xAI) — designer/implementer |
| **Human** | Zaheer ul Hassan |
| **Phase** | baseline |
| **Related changelog** | `[2026-08-29] — Phase 2: baseline one-pass fixer @ 37.5%` |
| **Status** | submission-ready |

---

## 0) System state before this session
- Phase 1 sample world ready; pristine cases all FAIL (0/8)
- No API keys in environment / no `.env` yet
- Need a fair weak baseline that advanced can beat

## 1) Session goal (falsifiable)
**Done means:** baseline runner exists; scored on all 8 cases; metrics JSON saved; README/REPRO updated; trajectory + push.

## 2) Constraints
- Official PDF allows “simple script” as baseline
- Coding-agent use required for the overall submission (Grok builds; advanced phase adds productized agent loop)
- Must not mutate pristine `sample_world/cases` (copy to work/)
- Do not encode fixes for all 8 bugs (would make baseline too strong)

## 3) Plan (before coding)
1. Implement one-pass heuristic fixer with max one edit + one retest
2. Optional LLM oneshot module for when keys exist
3. Run eval; record JSON
4. Docs + changelog + trace + commit

## 4) Agent brief (prompt issued)
```
Start Phase 2
```

**Why this brief:** Produce measurable baseline success rate before advanced work.

## 5) Execution log
| Step | Actor | Action | Result |
|------|-------|--------|--------|
| 1 | agent | Check API keys | none present |
| 2 | agent | Choose script baseline over blocked LLM-only path | ok |
| 3 | agent | Implement `BASELINE/fix_once.py` | ok |
| 4 | agent | Implement optional `agent_llm_oneshot.py` | ok |
| 5 | agent | Run `python BASELINE/fix_once.py` | **3/8 = 37.5%** |
| 6 | agent | Gitignore `BASELINE/work/`; commit metrics JSON | ok |

**Commands / checks run:**
```bash
python3 BASELINE/fix_once.py
# baseline success_rate=37.50% (3/8)
```

## 6) Verification
- [x] Same 8 eval cases as Phase 1
- [x] Pristine fixtures untouched (work copies)
- [x] Metrics artifact written
- [x] Success rate strictly between 0% and 100% (room for advanced)

**Evidence:** `BASELINE/results/baseline_metrics.json`

## 7) Decision record
| Decision | Chose | Rejected | Why |
|----------|-------|----------|-----|
| Baseline type | One-pass heuristics script | Wait for API keys / LLM-only | Unblocks scoring; PDF explicitly allows simple script |
| Heuristic coverage | 3 shallow patterns only | Full oracle of all bugs | Keep baseline weak/fair for measured improvement |
| Spoiler handling | Delete `meta.json` in work copy | Leave spoilers in workspace | Reduce accidental cheating by later agents |

**Tradeoff accepted:** Heuristic baseline is not itself an LLM agent; overall submission still agent-driven via Grok trajectories + Phase 3 advanced agent.  
**Revisit if:** Organizer clarification requires LLM in baseline specifically — then run `agent_llm_oneshot.py` once keys are set and report side-by-side.

## 8) Failure / incident
None blocking. Note: some failing cases still pass a subset of tests (e.g. discount 10% accidentally equals `price - 10`) but suite remains red overall.

## 9) Risk still open
- Advanced must clear semantic cases (`02–05`, `08`) to show clear lift
- LLM oneshot path unverified until Zaheer adds API key

## 10) Handoff
**Next session starts with:** Phase 3 — advanced plan→edit→test→retry agent targeting >37.5% success rate on the same cases.

---

### Quality gate
- [x] Goal clear and checked
- [x] Prompt preserved
- [x] Actions + evidence present
- [x] Decision/tradeoff recorded
- [x] Honest about no API keys
- [x] Handoff actionable
- [x] No secrets
